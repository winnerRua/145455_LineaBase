@using WAREGBOGWMM.Models
@model WAREGBOGWMM.Models.Usuario
@{
    ViewBag.Title = "Alta/Baja Usuarios";
}

@using (Ajax.BeginForm("AltaBajaUsuario1", "GestionUsuarios", new AjaxOptions() { OnSuccess = "alert('finalizo');", OnBegin = "alert('cargando');" }, new { @id = "AltaBajaUsuarios1-form" }))
{
    @Html.AntiForgeryToken()
    <h2>Habilitar/Inhabilitar usuario</h2>
    <div class="container">
        <br />
        <div class="row">
            <div class="row form-inline">
                @Html.LabelFor(model => model.nombreUsuario)
                @Html.TextBoxFor(model => model.nombreUsuario, new { @class = "form-control input-lg place", @autocomplete = "off" })
                @Html.ValidationMessageFor(model => model.nombreUsuario)
                <input type="button" onclick="EstateCurrentUser('@Url.Action("ConsultaUsuario", "GestionUsuarios")');" class="btn btn-primary" value="Buscar usuarios" />
            </div>
        </div>

        <br />
        <div class="row">

            <div class="col-sm-3">
                @Html.LabelFor(model => model.estado)
                @Html.TextBoxFor(model => model.estado, new { @class = "form-control input-lg", @autocomplete = "off" })
                @Html.ValidationMessageFor(model => model.estado)
            </div>
        </div>
        <br />
        <input type="submit" class="btn btn-primary" value="Actualizar usuario" />
    </div>
}
@if (ViewBag.Message != null)
{
    <script type="text/javascript">

            window.onload = function () {
                alert("@ViewBag.Message");
            };
    </script>

}
@section scripts
{
    @Scripts.Render("~/bundles/jqueryval")
}